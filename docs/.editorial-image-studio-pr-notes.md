# Editorial Image Studio PR Notes

This branch exposes imported `editorialImage` records in Sanity Studio and supports manual Rugby Panda original uploads.

## Verification after deployment

- Studio loads.
- Editorial Images appears in Studio structure.
- 16 imported records are visible.
- Imported image thumbnails render from stored URLs.
- Editors can update lifecycle status, approval, rating, category, photo type and tags.
- Manual uploads can be added to the same collection.
- Rugby Panda originals use public credit `Photo: The Rugby Panda` and copyright `© The Rugby Panda`.
