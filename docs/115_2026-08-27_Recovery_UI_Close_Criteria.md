# WEB-013 close criteria

Close only when: recovery code merged; exact production deployment READY; owner reloads authenticated Sanity on phone; Stored formatted preview body/heading/bold/list text is clearly readable; no new mobile interaction regression observed.
