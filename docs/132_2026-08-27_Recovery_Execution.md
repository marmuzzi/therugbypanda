# Recovery execution

Interactive execution is active. This branch demonstrates the required pattern: inspect exact production symptom -> find exact source root cause -> implement -> commit -> PR -> deploy -> verify. Apply the same pattern to freshness and imagery next.
