# Editorial Review Raw Perspective Verification

The queue fix requires the hosted Sanity Studio to use an authenticated non-CDN `raw` perspective because the queue selects `drafts.*` IDs directly. Production verification is complete only when a manually created unpublished article appears in Editorial Review and can be submitted through the controlled workflow.
