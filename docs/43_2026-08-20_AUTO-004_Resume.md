# AUTO-004 Resume — 20 August 2026

## Purpose

Resume The Rugby Panda from the production state left on 18 August 2026 without restarting AUTO-001 or repeating already-verified work.

## Source-of-truth state

- AUTO-001 remains production verified and must not be restarted.
- PR #176 is merged and introduced deliberate all-source synthesis plus fail-closed Editorial Image relevance.
- PR #179 is merged and introduced precision, source-metadata-only Apify image relevance and capped exact-subject acquisition planning.
- PR #183 is merged and introduced genuinely independent source packs plus a deterministic fail-closed originality gate.
- PR #184 is merged and closes the P1 gap by protecting raw `story.title`, `story.summary` and `story.bodyText` as well as structured source records in the originality comparison.
- Current `main` includes `SourceRecord.bodyText?: string` and the complete originality protection.
- A later PR #184 preview deployment built READY, proving the final code path compiles.
- The last production-target deployment visible in Vercel is still the earlier #181 deployment; current `main` has not yet been production verified.

## Immediate resume sequence

1. Use this documentation-only PR to trigger a fresh Git-linked deployment from current `main` now that the rolling Vercel free-plan deployment window has had time to clear.
2. Verify the resulting `main` deployment is `READY` and production-targeted.
3. Run `data/editorial-acquisition/auto004-2026-08-18-independent.json` through the existing controlled AUTO-004 import path.
4. Confirm all five generated drafts pass the deterministic originality gate before any Sanity write.
5. Inspect the actual article prose for material independence from every source, not merely low exact phrase overlap.
6. Verify PR #176 image selection remains relevance-first and fail-closed.
7. Verify package-mode suppression still prevents five individual morning draft emails and that only the consolidated AUTO-001 package is sent when the controlled package is executed.
8. Update `docs/07_Project_State.md`, `docs/08_Issue_Log.md` and this handoff with deployed/verified results.

## Originality release blocker

No AUTO-004 article is production-ready merely because it is factually correct or multi-source. It must also be materially original in wording, structure, transitions and analysis. The deterministic guard is a hard pre-Sanity barrier, and editorial inspection remains required after the automated check.

## Media cost rule

Do not launch another broad paid Apify image crawl. When media work resumes, use the exact-subject precision plan from PR #179, start with a small Tier-1 sample, and measure useful-image yield before expanding.
