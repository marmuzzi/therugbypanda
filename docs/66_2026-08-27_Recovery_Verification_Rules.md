# Recovery verification rules

Do not equate implemented with merged, merged with deployed, deployed with production-verified, or automated production evidence with owner-visible acceptance. Every recovery report must state those states separately. A stale daily package, unreadable authenticated CMS surface or visibly irrelevant image is a failed gate even if prior automation reported success.
