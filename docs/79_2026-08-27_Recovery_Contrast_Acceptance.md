# Contrast acceptance

The formatted preview intentionally uses a white proofing surface. Therefore all text rendered inside it must explicitly use dark high-contrast foregrounds independent of Sanity theme. Text outside the proofing surface should follow Sanity theme variables. This prevents the dark-theme inheritance bug while keeping article proofing visually predictable.
