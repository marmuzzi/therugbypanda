# UI recovery risk

Low runtime risk: the proof surface was already forced white, so explicitly forcing dark foreground within that same surface removes theme ambiguity rather than changing theme semantics. Surrounding helper surfaces move toward Sanity theme variables. Build and authenticated phone verification still mandatory.
