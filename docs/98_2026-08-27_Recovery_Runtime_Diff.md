# Runtime diff

Before: preview `background:#fff`; normal Portable Text inherited active theme text; helper surfaces hard-coded greys/`#fafafa`.

After: preview `background:#fff`, `color:#171717`, `colorScheme:light`; blocks/lists/links explicitly readable on light surface; surrounding helper copy/surface follows Sanity theme variables.
